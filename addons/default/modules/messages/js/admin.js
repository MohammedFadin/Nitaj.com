jQuery(function($){
	
	// generate a slug when the user types a title in
	pyro.generate_slug('input[name="major_name"]', 'input[name="slug"]');
	pyro.generate_slug('input[name="course_name"]', 'input[name="slug"]');
	pyro.generate_slug('input[name="material_name"]', 'input[name="slug"]');

	
});